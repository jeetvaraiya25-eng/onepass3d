Sample orbit: Ignatius garden

Original file: /Users/jeet/Downloads/Ignatius.mp4

Files here:
  ignatius-web.mp4      720p, no audio, 25 MB — what the site plays and offers
                        for download. Committed, so a hosted build has it too.
  ignatius-poster.jpg   first-frame poster, so the page does not stream the
                        video until someone presses play.
  ignatius.mp4          full 1080p copy, 198 MB. Local only (gitignored).
  *.splat               example scenes, local only (gitignored). /api/examples
                        reports ready:false for any that are missing, and the
                        site then says the scene is not on this server.

Keep filenames lowercase: the Mac ignores case but a Linux host does not.
